# Direct dependencies used for local verification. Transitive packages are
# resolved by pip; this is a tested constraints list, not a full lockfile.
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.28.1
numpy==2.2.6
scipy==1.14.1
tigramite==5.2.10.1
joblib==1.4.2
PyYAML==6.0.3
pytest==9.0.3
